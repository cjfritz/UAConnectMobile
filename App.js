import React from 'react';
import { Container, Header, Title } from 'native-base';

const App = () => (
  <Container>
    <Header>
      <Title>Hello, World!</Title>
    </Header>
  </Container>
);

export default App;
